class Solution {
    public boolean checkIfPangram(String sentence) {
        int flag=0;
        for ( char i= 'a'; i<='z'; i++) {
            if(sentence.contains(i+"")) {
                flag++;
            }
        }
        if(flag==26)
            return true;
        else
            return false;
    }
}
